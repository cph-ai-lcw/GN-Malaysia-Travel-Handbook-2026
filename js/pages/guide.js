import {FOODS,SHOPPING} from '../data.js';import {bi} from '../i18n.js';
export function guidePage(){return `<section class="section"><h1>${bi('美食・購物攻略','Ăn uống・Mua sắm')}</h1><div class="card notice"><b>🏆 ${bi('團體唯一夜市：亞羅街','Chợ đêm duy nhất: Jalan Alor')}</b><p>${bi('團體遊覽車上下客較方便，請依領隊指定時間與地點集合。','Xe đoàn lên xuống thuận tiện hơn. Tập trung theo giờ và địa điểm của trưởng đoàn.')}</p></div></section><section class="section"><div class="section-head"><h2>${bi('必吃美食','Món nên ăn')}</h2></div><div class="grid">${FOODS.map(x=>`<div class="card food-card"><span class="emoji">${x.icon}</span><div><h3>${bi(x.nameZh,x.nameVi)}</h3><small>📍 ${x.place}</small><br><span class="badge">${x.price}</span></div></div>`).join('')}</div></section><section class="section"><div class="section-head"><h2>${bi('伴手禮・購物','Quà tặng・Mua sắm')}</h2></div><div class="grid">${SHOPPING.map(x=>`<div class="card shop-card"><span class="emoji">${x.icon}</span><div><h3>${bi(x.nameZh,x.nameVi)}</h3><small>📍 ${x.where}</small><br><span class="badge">${x.price}</span></div></div>`).join('')}</div></section>`}
